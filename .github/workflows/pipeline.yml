name: pipeline

on:
  pull_request:
    types: [closed]
    paths:
      - "datasets/**"
      - "support/**"
      - "scripts/**"
      - ".github/workflows/pipeline.yml"
  schedule:
    # Mensile: 5 del mese alle 6:00 UTC
    - cron: "0 6 5 * *"
  workflow_dispatch:

concurrency:
  group: siope-pipeline-${{ github.event.pull_request.number || github.ref || 'schedule' }}
  cancel-in-progress: false

jobs:
  pipeline:
    uses: dataciviclab/.github/.github/workflows/pipeline-reusable.yml@main
    with:
      repo-slug: siope
      run-command: make run-all
      verify-command: make verify
      extra-env: |
        DUCKDB_MEMORY_LIMIT=1.5GB
        DUCKDB_THREADS=2
        DUCKDB_PRESERVE_INSERTION_ORDER=false
    secrets: inherit
